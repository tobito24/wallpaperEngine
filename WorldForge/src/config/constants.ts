export const TILE_SIZE_DEFAULT = 32;
export const MIN_TILE_SIZE = 8;
export const MAX_TILE_SIZE = 256;

export const CHUNK_SIZE = 16;

/** Source pixel size of one sprite cell inside tileset.png — independent of the on-screen zoom (Camera.tileSize). */
export const TILESET_TILE_SIZE = 32;

/** Relative to public/, matches the InfinityWalker/WorldGenerator tileset layout (8 columns). */
export const TILESET_PATH = 'img/tileset.png';
